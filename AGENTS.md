# AGENTS.md

## Project

Python project using `pyproject.toml`. Requires Python 3.12+.

## Commands

```bash
python main.py          # Run the entrypoint
```

## Files

- `pyproject.toml` - project config, no dependencies defined yet
- `main.py` - entrypoint (currently just prints hello)

## Notes

- No tests, linting, or CI configured yet
- This is a new/minimal project scaffold